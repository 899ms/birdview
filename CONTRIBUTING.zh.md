# 文档维护

[English](CONTRIBUTING.md)

## 语言配对

仓库自有 Markdown 在同一目录中使用英文 `name.md` 和中文 `name.zh.md`，首个标题下放置互相对应的语言链接。翻译完整含义、示例、约束和限制，不用摘要替代译文。优先链接同语言的配对指引。代码标识、命令、数据路径和枚举值保持不变。

`SKILL.md` 保留原有 frontmatter，仍是可执行技能入口。`SKILL.zh.md` 为读者翻译指令，不注册第二个技能。规范的 `name` 和 `description` 仅放在 `SKILL.md`。语言切换链接是每页唯一有意保留的双语导航。

## 同步维护

两种语言必须表达相同要求。在同一次修改中编辑并审阅两份文件，包括互链和引用。用户指令优先于任一版本。有差异时，根据实现和已授权需求解决，再修正两种语言，不要静默将某种语言视为更权威。

`docs/i18n.json` 记录各文件最后一次确认一致时的 SHA-256。计算前将 CRLF 规范化为 LF，兼容 Windows 检出。运行：

```sh
node scripts/check-docs.mjs
```

仅在核对两种译文后更新记录：

```sh
node scripts/check-docs.mjs --update
```

成对文档和记录一起提交。检查能发现缺少配对、缺少语言链接、损坏的本地 Markdown 链接，以及自确认后变化的内容，但不能判断翻译准确性。不要只为消除报错而刷新哈希。依赖和生成输出不属于此文档规则。根目录、`references/`、`docs/` 或 `examples/` 内新增自有 Markdown 必须配对；增加其他自有文档目录时同步扩大检查范围。
